# Releases

Releases are handled via the GitHub Actions workflow `Release`.

## How to Release

1. Go to **Actions** → **Release**.
2. Enter a version like `v1.0.0`.
3. Add optional release notes.
4. Run the workflow.

This will create a Git tag and GitHub Release automatically.
